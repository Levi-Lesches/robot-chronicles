import "oauth.dart";

class Mln {
  static Future<void> grantReward(AccessToken accessToken, int rewardID) async {
    // Temporary
    // ignore: avoid_print
    print("  GRANTED REWARD IN MLN: $accessToken now has $rewardID");
  }
}
